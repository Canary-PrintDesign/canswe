#!/bin/bash

#if [ $ENV = 'production' ]; then
#  JEKYLL_ENV="$ENV" bundle exec jekyll build --config 'src/_config.yml,src/_config-common.yml,src/_config-production.yml' --source src/ --destination dist/
#else
#  bundle exec jekyll build --watch --config 'src/_config.yml,src/_config-common.yml,src/_config-development.yml' --source src/ --destination dist/ --incremental
#fi
